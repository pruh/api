version: '3.8'

services:
  api:
    build: .
    container_name: api
    restart: unless-stopped
    ports:
      - 8081:8080
    volumes:
      - /etc/localtime:/etc/localtime:ro
    env_file: api.env
